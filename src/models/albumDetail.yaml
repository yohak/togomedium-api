type: object
properties:
  id:
    type: integer
  work_result_id:
    type: integer
  album_id:
    type: integer
  run_task_id:
    type: integer
  document:
    type: string
    description: 現時点ではバイタル関係の入力値のみに使用されている
