get:
  summary: gmdb_organisms_by_phenotypes
  description: 様々な表現型情報から生物種を検索する
  responses:
    200:
      description: SUCCESS
      content:
        application/json:
          example:
            $ref: "../examples/organism-list.json"
          schema:
            type: array
            items:
              type: object
              properties:
                tax_id:
                  type: string
                name:
                  type: string
