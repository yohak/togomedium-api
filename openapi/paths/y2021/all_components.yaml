get:
  summary: gmdb_all_components
  tags:
    - Media Finder
    - Y2021
  description: すべての成分を返す
  responses:
    200:
      description: SUCCESS
      content:
        application/json:
          example:
            $ref: "../../examples/all-components.json"
          schema:
            type: array
            items:
              type: object
              properties:
                gmo_id:
                  type: string
                name:
                  type: string
